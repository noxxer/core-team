---
role: "role_name"
last_updated: "YYYY-MM-DD"
max_lines: 200
---

# [Role]: Private Context

> **Это СОСТОЯНИЕ роли, а не журнал.** Файл отвечает на вопрос «что я знаю и где я
> сейчас», а не «что происходило по дням». Датированные записи о ходе работ живут в
> `project/sessions/` — там у них своё место и своя ротация.
>
> **Почему это инвариант, а не вкусовщина.** Дописать строку дешевле, чем переписать
> раздел, поэтому лента всегда побеждает состояние, если её пустить. У ленты нет дна:
> в боевом проекте память `dev` доросла до 571.5 КБ (4980 строк, 78 датированных
> блоков) при пределе чтения инструмента **256 КБ** — и роль перестала физически
> открывать собственную память. Ритуал активации при этом исполнялся: агент не падал,
> он молча работал без памяти и дописывал в файл, которого не видел.
>
> Потолок — `max_lines` в шапке (умолчание 200). Поднимается осознанно и с причиной,
> а не молчаливым ростом. Проверяет `hooks/check-role-memory.sh`.

## Текущий фокус

[Над чем роль работает сейчас и почему именно над этим. 3-5 строк, переписывается целиком.]

## Рабочие знания

[Ключевые фреймворки, методы, экспертиза роли. Устойчивое знание, а не события:
«повторные попытки живут в слое синхронизации, а не в вызывающем коде» — да;
«во вторник починил повторные попытки» — нет.]

## Процессные заметки

[Как эта роль взаимодействует с другими: с кем сверяется, где типично возникает трение,
какие хэндофы работают, а какие ломались.]

## Открытые вопросы по домену

[Нерешённые tensions в зоне ответственности роли. Закрытый вопрос отсюда УДАЛЯЕТСЯ —
его след остаётся в `project/sessions/` и `project/decisions/`.]

## Последние решения

[DEC-NNN, затрагивающие роль — одна строка на решение, только действующие.
Отменённое решение убирается: append-only живёт в `project/decisions/`, здесь — срез.]

## Уроки ремесла

[То, что куплено дорого и обязано пережить сессию: «мутация откатывается точечной правкой»,
«проверять файлом, а не отчётом». Именно ради этого раздела память и читается —
не давай ему утонуть в хронике.]
