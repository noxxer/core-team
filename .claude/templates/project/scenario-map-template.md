# Scenario Map — карта сценариев продукта

**Owner:** product
**Update-триггеры:** новый DEC | новый pipeline-step | новый внешний API | изменение flow оплаты/авторизации/хранения данных
**Last updated:** YYYY-MM-DD

> Каждый экран продукта имеет 5 состояний (Scott Hurff). Каждый системный сбой имеет определённое поведение. Все пробелы — здесь.

---

## 5 состояний UI — таблица по экранам

| Экран | Loading | Empty | Error | Success / Ideal | Partial |
|-------|---------|-------|-------|-----------------|---------|
| `<screen-1>` | <skeleton/spinner> | <что говорим, что предлагаем> | <человекочитаемое сообщение + recovery> | <основной контент> | <pagination в процессе / частичная загрузка> |
| `<screen-2>` | ... | ... | ... | ... | ... |

**Правило:** ни один state не «TBD» — для каждого должен быть осознанный ответ. `N/A` допустимо с пояснением «этот state на этом экране невозможен потому что Y».

---

## System Failure Map — поведение при системных сбоях

| Сбой | Что видит пользователь | Что делает система | Recovery |
|------|------------------------|--------------------|---------|
| Backend 5xx | <сообщение> | <retry / queue / fail-fast> | <ручной retry / автоматический> |
| Внешний API недоступен | ... | ... | ... |
| Сеть offline | ... | ... | ... |
| 404 на ресурсе | ... | ... | ... |
| Loading timeout (>X сек) | ... | ... | ... |
| Файл превышает лимит | ... | ... | ... |
| Webhook не пришёл | ... | ... | ... |
| Auth token expired | ... | ... | ... |

---

## Граничные случаи и edge scenarios

| Сценарий | Ожидаемое поведение | Кто owns |
|----------|---------------------|----------|
| Пустые данные везде | ... | product |
| Пользователь без прав | ... | product/guardian |
| Все API одновременно упали | ... | guardian |
| Дублирующийся submit | ... | dev |

---

## Pivot-точки (kill criteria для сценариев)

Если для конкретного сценария прописано kill-criterion (см. `project/decisions/DEC-NNN`) — отметить здесь:

- `<сценарий>` — kill при <метрика порог>, owner: guardian, watchlist: `scorecard.md`

---

## Связанные DEC

- DEC-NNN — какой принцип сценариев заложен
- DEC-NNN — конкретный сценарий
