---
last_updated: "YYYY-MM-DD"
last_session: null
active_phase: "setup"          # ОДНА строка: имя фазы, не пересказ сессии
project_name: "PROJECT_NAME"
max_lines: 300                 # потолок файла; поднимается осознанно и с причиной
code_path: ""                  # путь к репозиторию кода, если он отдельный от этого
ship_max_ahead: 20             # сколько коммитов допустимо держать неотгруженными
ship_max_days: 14              # сколько дней допустимо не отгружать
findings_ceiling: 20           # сколько находок низкой ставки проект держит открытыми
---

<!--
ФОРМА ЭТОГО ФАЙЛА — КОНТРАКТ, и его проверяет `hooks/check-ledger.sh`:

  · нет разделов, привязанных к дате или сессии — события живут в `project/sessions/`;
  · ни одно поле шапки и ни одна строка лестницы смыслов не длиннее 200 символов;
  · нет нумерованных полей вида `previous_phase_171` — это лента без дна;
  · есть все три строки лестницы смыслов;
  · строк не больше `max_lines`.

Замер на четырёх боевых проектах (2026-08-26): 320 / 427 / 473 / **3682** строки;
лестницы смыслов нет в трёх из четырёх; в двух хроника ушла в ШАПКУ —
`active_phase` 4141 символ, `current_omtm` 2340 — плюс нумерованные
`previous_phase_171`, `previous_phase_170`. Ledger читают все (6 команд, 6 ролей,
4 гарда), поэтому в него дописывают всё: он самый полезный и потому самый уязвимый.
-->

# Project Ledger

## Зачем

> Лестница смыслов: три строки, которые печатает `hooks/session-start.sh` в начале каждой сессии.
> **Одна строка на уровень.** Поле длиннее 200 символов помечается как хроника: события живут
> в `session.md`, здесь — смысл. Измеренная болезнь: в боевом проекте поле фазы за 172 сессии
> превратилось в пересказ последней сессии, и цель фазы перестала существовать.

- **Миссия:** [зачем существует проект — меняется редко]
- **Цель фазы:** [что должно стать правдой к концу фазы — и почему именно это сейчас]
- **Ближайший шаг:** [что делаем в ближайшую сессию; синхронно с handoff]


## Текущая фаза: Setup
Проект инициализирован. Первая сессия.

## Контекст
[Заполнить при setup-project]

## Команда
| Роль | Статус | Фокус |
|------|--------|-------|
| Facilitator | Active | Инициализация |

## Активные задачи
- [ ] Провести первую рабочую сессию
- [ ] Определить фокус первой фазы

## Недавно завершено

> **Только последняя сессия.** Всё, что раньше, уже лежит в `project/sessions/` —
> здесь оно было бы вторым описанием, а второе всегда отстаёт от первого.
> Замер: в боевом проекте этот раздел дорос до 108 строк.

- [x] Setup проекта

## Блокеры
Нет

## Открытые вопросы
- OQ-01: [Первый вопрос проекта]

## Ключевые решения
—

## Фазы
| Фаза | Статус | Описание |
|------|--------|----------|
| Setup | Active | Инициализация проекта |
