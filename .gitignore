# Техничка миграции/сборки — не для публикации
_migration/

# Локальные машинные настройки и разрешения
.claude/settings.local.json

# Рантайм проекта-потребителя (создаётся /setup-project, не часть фреймворка)
project/

# OS / IDE
.DS_Store
.idea/
*.swp
