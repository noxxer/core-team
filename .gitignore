# Техничка миграции/сборки — не для публикации
/_migration/

# Локальные машинные настройки и разрешения
.claude/settings.local.json

# Память планировщика проекта-потребителя: лежит в .claude/ по историческим
# причинам, но это ПАМЯТЬ, а не инструмент, и в поставку ехать не должна.
.claude/planner-context.md

# Рантайм проекта-потребителя (создаётся /setup-project, не часть фреймворка).
# Якорь "/" обязателен: неякорёный "project/" матчил бы и .claude/templates/project/,
# из-за чего шаблоны не попадали в публикацию (баг 4.5.0, фикс 4.5.1).
/project/

# OS / IDE
.DS_Store
.idea/
*.swp
